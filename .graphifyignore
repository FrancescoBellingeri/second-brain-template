# What: paths graphify must never index (gitignore syntax). Applies to every
#   flow — detect, `graphify update`, `graphify extract`, the /graphify skill.
#   .obsidian/ holds Obsidian's own config + workspace JSON: noise, not knowledge.
#   (graphify-out/ and venv/ are already skipped by graphify's defaults.)
# Test: after adding, `graphify update .` and detect must list ZERO .obsidian files.
.obsidian/

# Vault config/docs are not knowledge — exclude from the optional /graphify
# enrichment run so it only reads real notes (fewer tokens). The deterministic
# kepra-index reads projects/naming.md directly, not via graphify.
README.md
CLAUDE.md
templates/
projects/naming.md
